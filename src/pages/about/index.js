import React, { memo } from "react";
import styled from "styled-components";

const index = memo(() => {
    return (
        <div>
            <h2>index</h2>
            
        </div>
    );
});

export default index;